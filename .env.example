# Optional future OpenAI integration.
# The current project uses a deterministic mock AI recommendation engine.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini

# Server configuration
PORT=5000
CLIENT_ORIGIN=http://localhost:5173
